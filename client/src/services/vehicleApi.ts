// This file is for: Vehicle API service — CRUD + availability + stats
// Module: Frontend API Services (Module 10)
// Owner: Developer 2 (Web Frontend Engineer)
//
// What goes here:
// - getVehicles(filters) → GET /vehicles
// - getAvailableVehicles() → GET /vehicles/available
// - getVehicleStats() → GET /vehicles/stats
// - createVehicle(data) → POST /vehicles
// - updateVehicle(id, data) → PUT /vehicles/:id
// - updateVehicleStatus(id, status) → PATCH /vehicles/:id/status
// - deleteVehicle(id) → DELETE /vehicles/:id
